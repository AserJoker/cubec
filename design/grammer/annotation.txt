annotation_param => 
    literal_string|
    literal_number|
    literal_identifier<'true'>|
    literal_identifier<'false'>|
    literal_identifier<'null'>
annotation =>
    literal_symbol<'[['>
    space?
    literal_identifier
    (
        space?
        literal_symbol<'('>
        (
            annotation_param
            (
                space?
                literal_symbol<','>
                annotation_param
            )*
        )?
        literal_symbol<')'>
    )?
    space?
    literal_symbol<']]'>