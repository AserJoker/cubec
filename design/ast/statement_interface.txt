interface_item =>
    expression_type
interface_item =>
    literal_identifier
    space?
    literal_symbol<':'>
    space?
    expression_type
statement_interface =>
    literal_identifier<'interface'>
    space
    literal_identifier
    (
        space?
        literal_symbol<'<'>
        space?
        generic_declarator
        (
            space?
            literal_symbol<','>
            generic_declarator
        )*
        space?
        literal_symbol<'>'>
    )?
    (
        space
        literal_identifier<'extends'>
        expression_type
        (
            space?
            literal_symbol<','>
            expression_type
        )*
    )?
    space?
    literal_symbol<'{'>
    (
        interface_item
        space?
        literal_symbol<';'>
    )*
    literal_symbol<'}'>
    space?
    literal_symbol<';'>