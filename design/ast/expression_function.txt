
function_boody =>
    statement_if|
    statement_for|
    statement_while|
    statement_dowhile|
    statement_switch|
    statement_match|
    statement_return|
    statement_label|
    statement_defer|
    statement_declaration|
    statement_expression|
    statement_type|
    statement_empty

func_type =>
    literal_identifier<'func'>|
    literal_identifier<'meta'>|
    literal_identifier<'get'>|
    literal_identifier<'set'>|
    literal_identifier<'async'>

expression_function =>
    func_type
    (
        space?
        literal_identifier
    )?
    space?
    literal_symbol<'('>
    (
        space?
        literal_identifier
        space?
        literal_symbol<':'>
        space?
        expression_type
        (
            space?
            literal_symbol<','>
            space?
            literal_identifier
            space?
            literal_symbol<':'>
            space?
            expression_type
        )*
    )?
    literal_symbol<')'>
    space?
    literal_symbol<':'>
    space?
    expression_type
    (
        space?
        literal_symbol<','>
        space?
        expression_type
    )*
    space?
    literal_symbol<'{'>
    space?
    function_boody
    space?
    literal_symbol<'}'>